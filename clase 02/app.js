//const let var//

function resta(a,b) {
    return a - b;
}

const restaReal = (a, b) => a-b;


//ahora con template strings

function saludar(nombre){
    return `hola ${nombre}`
}

const saludarVerdaderamente = (nombre) => `hola ${nombre}`

console.log(saludarVerdaderamente("fredox"));